---
node: pr:22
type: pr
title: we have a lot of docs now about wye, but it is a bit trash structured, refactor it to be…
status: draft
owner: unassigned
last-verified: 2026-09-20
session: d4d72bd04a
agent: claude-code
started: 2026-09-20T10:02:14.464Z
role: librarian
part-of: module:v2-prs
---

# we have a lot of docs now about wye, but it is a bit trash structured, refactor it to be…

## Request

> we have a lot of docs now about wye, but it is a bit trash structured, refactor it to be nice and clean definition of wye product

## Context

_What the agent found — modules, documents, nodes and code the request touches — as tags and embeds._

## Definition

_The blocks this request proposes — requirements, decisions, constraints, questions, tasks, edits of existing nodes — defined in their home documents and embedded here; the plan is defined when every one is agreed._

![[decision:wf2.definition-shape]]

![[decision:wf2.superseded-layers]]

![[decision:wf2.definition-leftovers]]

![[req:wf2.definition.one-place-per-kind]]

![[req:wf2.definition.what-wye-is]]

![[req:wf2.definition.links-survive]]

![[req:wf2.definition.pages-hold-only-definition]]

![[question:wf2.definition.id-prefixes]]

![[question:wf2.definition.constitution-status]]

```yaml
- id: task:wf2.definition.wye-page
  text: >
    Turn project.md (module:wf2) into the Wye page: purpose rewritten for what runs today (no clerk / SQLite),
    the goals (goal:exec.define-first, goal:exec.work-and-impact, goal:memory.validated-asks, goal:ontology.graph-editor)
    embedded, the constitution, the product-level decisions (ADRs, decision:wf2.product-model, the decisions from
    memory-review and ontology-design that concern the whole product), one line per area linking its PRD section and
    Design page; gates, drift and phases go to Archive or are closed.
  status: proposed
  part-of: pr:22
  home: none yet — move this block to the document where its kind lives
```

```yaml
- id: task:wf2.definition.prd
  text: >
    One PRD, a section per area: move every live requirement and open question from prd-execution, memory-review,
    ontology-design, the App sub-pages, dev-design and waterfall.md into prd.md under its area (ids unchanged);
    retire in place the `req&#58;wf2.*` that describe the unbuilt store / API / clerk / contradictions design; prd-execution
    becomes a section, its page retired.
  status: proposed
  part-of: pr:22
  home: none yet — move this block to the document where its kind lives
```

```yaml
- id: task:wf2.definition.design
  text: >
    A Design parent page with one module page per area — the seven App sub-pages retitled and moved under it, plus
    Ontology (from ontology.md + the decisions and rules of ontology-design) and Memory (the decisions and rules of
    memory-review); the live rules, pages, entities and ops of dev-design.md distributed by area; each page holds
    rules, decisions, components, libs, ops, flags and nothing else. app.md's "how it is put together" becomes the
    Design parent's prose.
  status: proposed
  part-of: pr:22
  home: none yet — move this block to the document where its kind lives
```

```yaml
- id: task:wf2.definition.tests-research-archive
  text: >
    Tests: test-design.md retitled, its Memory and Execution sections kept, retired tests marked. Research: a parent
    page holding memory-review, benchmarks and ontology-design as essays (their blocks moved out, prose and sources
    stay). Archive: a parent page outside the main tree holding waterfall.md, the emptied prd / dev-design pages and
    plan.md (module:wf2-plan), every remaining block in them marked retired.
  status: proposed
  part-of: pr:22
  home: none yet — move this block to the document where its kind lives
```

```yaml
- id: task:wf2.definition.leftovers
  text: >
    Move every `## Plan: …` section out of app-agents, app-knowledge and bugs into its plan document (or retire it
    there when the plan is done); mark done plan documents done; retitle every definition page to say Wye; keep ids,
    the product slug and the folder as they are.
  status: proposed
  part-of: pr:22
  home: none yet — move this block to the document where its kind lives
```

```yaml
- id: task:wf2.definition.verify
  text: >
    Before and after: record `wf packet --for` and `wf context` for five texts (one per major area) and compare —
    the same live nodes answer, only retired ones drop out; `ctx check --root data/products/waterfall` green with no
    dangling reference; every tag in every plan document and the Inbox still resolves; the Documents tree shows the
    seven top-level pages of decision:wf2.definition-shape.
  status: proposed
  part-of: pr:22
  home: none yet — move this block to the document where its kind lives
```

## Plan

_What was understood; questions as `question:` blocks, decisions as `decision:` blocks; the requirements and rules it proposes embedded from the pages they live on._

## Tasks

_`- [ ] task:` lines, `part of pr:22`; their check state is what is in progress._

- [ ] task:pr-22 we have a lot of docs now about wye, but it is a bit trash structured, refactor it to be… #review (worker: claude-code, session: d4d72bd04a, produced: pr:20 pr:18 pr:21 module:wf2-dev module:wf2 module:wf2-prd)

## Result

_Written by the app when the session ends: the summary and the blocks this plan produced._
