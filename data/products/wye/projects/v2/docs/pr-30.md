---
node: pr:30
type: pr
title: Smart import of markdown files and folders — documents first, then an agent extracts types and blocks
status: done
owner: alex
last-verified: 2026-09-21
finished: 2026-09-21T19:40:00.000Z
session: 017wTEs8Jy8fzwycEec3ktJC
agent: claude-code
started: 2026-09-21T19:30:00.000Z
role: worker
part-of: module:v2-prs
scope: [req:wf2.import.markdown, req:wf2.import.analyse, decision:wf2.import-lands-first-agent-rewrites-in-place, unless:wf2.import.markdown, then:wf2.import.markdown, when:wf2.import.markdown, req:wf2.ui.node-page, unless:wf2.import.analyse, then:wf2.import.analyse, when:wf2.import.analyse, consequence:wf2.import-lands-first-agent-rewrites-in-place, alternative:wf2.import-lands-first-agent-rewrites-in-place, context:wf2.import-lands-first-agent-rewrites-in-place, choice:wf2.import-lands-first-agent-rewrites-in-place, decision:wf2.hooks-and-skills, rule:inbox-review]
scope-of: 59fcd3e3
---

# Smart import of markdown files and folders — documents first, then an agent extracts types and blocks

## Request

> another feature - i want to be able to import md files to the app, or even folders with md, it should be smart import, using agent, read the file, extract types and instances, convert required blocks into types, requirements, facts, etc

*from: the person, in a Claude Code session · refs: module:req-documents, module:app-documents*

## Context

Import lands on what exists: lib:import (markdown → blocks) and the document route (op:api.doc.create) for writing pages; skills as documents (decision:wf2.hooks-and-skills) with `role`, `takes`, `writes`; hooks whose `task "…" --worker agent --skill skill:x` starts a session with a skill on a node (hook:req-approved-tests is the shipped example); the Inbox as the view of every proposed block and every edit (rule:inbox-review); `wye type add` and `wye node add` for types and instances (decision:ontology.collection-document); `wye doc write --section` with the hash check for the rewrite. The person chose: one document per file in the current project (a folder's tree kept as part-of), the agent rewrites the document in place with everything proposed, and the agent runs automatically through a hook.

## Definition

![[req:wf2.import.markdown]]

![[req:wf2.import.analyse]]

![[req:wf2.import.code]]

![[decision:wf2.import-lands-first-agent-rewrites-in-place]]

## Impact

Touches the Documents area only: a new route, a new library, a new rail dialog and drop zone, a new base skill and a shipped hook; the document status vocabulary gains imported / raw / analysed. No existing behaviour changes.

## Tasks

- [x] task:pr-30.import-lib lib:import-docs — paths → documents: slug from the title or file name with a numeric suffix on clash, front matter merge (node, type, title, status, source, part-of), folder tree → parent documents, relative images → assets; tests. part of pr:30
- [x] task:pr-30.import-route op:api.import — multipart POST with paths kept; writes through lib:import-docs; rebuilds; returns what landed and what was skipped. part of pr:30
- [x] task:pr-30.import-ui component:import-docs — Import… on the Documents +, a drop zone on the rail for files and folders, the dialog (project, parent, Analyse with agent), the result. part of pr:30
- [x] task:pr-30.import-skill skill:import from prompts/import.md, in BASE_SKILLS; hook:import-analyse on the shipped Hooks page and on Wye's own. part of pr:30
- [x] task:pr-30.import-code op:api.import-code — From code in the same dialog and `wye import --code`: lib/init.js `--feature` writes the layered definition, each describe task handed to the default agent with skill:describe-module (req:wf2.import.code). part of pr:30
- [x] task:pr-30.import-cli `wye import <file|dir> --product p [--project x] [--parent doc] [--no-analyse]`. part of pr:30
- [x] task:pr-30.import-analyse-button an imported or raw document shows Analyse on its page (runs the hook on the node). part of pr:30
- [x] task:pr-30.import-e2e the skill run once on a scratch product with three real files; the result reviewed. part of pr:30

## Result

Built in session:017wTEs8Jy8fzwycEec3ktJC. lib:import-docs (pure plan + write, 6 tests), op:api.import (multipart, the tree kept, images to assets), op:api.import-code (init `--feature` + describe tasks to the agent), component:import-docs (Import… on the Documents head with Markdown / From code modes, a file drop zone on the rail), component:imported-notice (Analyse with agent on an imported or raw page — a raw page is marked imported first so the hook's `where` holds), skill:import (prompts/import.md, a base skill), hook:import-analyse (shipped Hooks page and Wye's own), `wye import`. `wye --help` now prints the whole command list (it stopped at line 40 before).

Run once for real on a scratch product with three files (a PRD, a standup note with an image, a glossary): the folder tree landed as documents (folder page, image in assets), the hook started session b9fa231764 on the PRD, and the agent rewrote it in place — 6 req with when/then, 1 decision with context and alternative, 1 setting, 1 fact (type:fact proposed on the Ontology page), 2 questions (one about scope, citing the standup note as evidence) — every sentence kept, `wye check` 0 errors, all of it proposed in the Inbox. One defect found and fixed: the agent wrote the file back with its front matter, which `wye doc write` doubled; op:doc.update now merges a body's own front matter (write.ts#replaceBody).

Left open: a page's "Analyse" runs one session — a folder of fifty files means fifty sessions, throttled only by Settings › Agents parallel runners.
